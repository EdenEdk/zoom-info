import { AppPopupDirective } from './app-popup.directive';

describe('AppPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPopupDirective();
    expect(directive).toBeTruthy();
  });
});
